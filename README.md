# XYVEL Medical — dépôt de distribution

Ce dépôt héberge uniquement les **artefacts publics** de l'application XYVEL Medical :
- les **Releases** (exécutables `.exe` Windows, `.apk`/`.aab` Android)
- le **site web** (branche `gh-pages`)

Le **code source** est privé et maintenu dans un dépôt séparé.

— XYVEL Medical